-- ==========================================
-- MEOW — Secure HWID Locked Key System & UI
-- ==========================================

local hwid = ""
pcall(function()
    hwid = gethwid and gethwid() or identifyexecutor and identifyexecutor() or "UnknownDevice"
end)

local lockedFile = "meow_device_lock.txt"
local isAlreadyLocked = false
pcall(function()
    if isfile and isfile(lockedFile) then
        if readfile(lockedFile) == hwid then
            isAlreadyLocked = true
        end
    end
end)

local successGet, rawKeys = pcall(function()
    return game:HttpGet("https://raw.githubusercontent.com/krypticlabx/myloves-/main/04291à196fa0a9m76b630c1y16f86709")
end)

if not successGet then
    game:GetService("Players").LocalPlayer:Kick("\n[MEOW ERROR]\nHindi ma-load ang key database!")
    return
end

local HttpService = game:GetService("HttpService")
local ValidKeys = {}
pcall(function()
    ValidKeys = HttpService:JSONDecode(rawKeys)
end)

local successKey, input_key = pcall(function()
    return getgenv().script_key
end)

pcall(function()
    getgenv().script_key = nil
end)

if not successKey or not input_key or not ValidKeys[input_key] then
    game:GetService("Players").LocalPlayer:Kick("\n[ACCESS DENIED]\nWalang Script Key o Maling Key!")
    return
end

pcall(function()
    if writefile and not isfile(lockedFile) then
        writefile(lockedFile, hwid)
    end
end)

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local Player = Players.LocalPlayer

if CoreGui:FindFirstChild("MeowUI") then 
    CoreGui.MeowUI:Destroy() 
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MeowUI"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 200, 0, 130)
MainFrame.Position = UDim2.new(0.05, 0, 0.5, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(26, 26, 30)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 14)
MainCorner.Parent = MainFrame

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 38)
TitleBar.BackgroundColor3 = Color3.fromRGB(38, 38, 45)
TitleBar.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = "MEOW (SECURED)"
Title.TextColor3 = Color3.fromRGB(255, 215, 0)
Title.TextSize = 16
Title.Font = Enum.Font.GothamBold
Title.Parent = TitleBar

local StatusDot = Instance.new("Frame")
StatusDot.Size = UDim2.new(0, 10, 0, 10)
StatusDot.Position = UDim2.new(0, 15, 0, 50)
StatusDot.BackgroundColor3 = Color3.fromRGB(50, 220, 90)
StatusDot.Parent = MainFrame

local DotCorner = Instance.new("UICorner")
DotCorner.CornerRadius = UDim.new(0, 5)
DotCorner.Parent = StatusDot

local AutoRejoin = false
local AutoBtn = Instance.new("TextButton")
AutoBtn.Size = UDim2.new(0, 80, 0, 30)
AutoBtn.Position = UDim2.new(0, 15, 0, 70)
AutoBtn.BackgroundColor3 = Color3.fromRGB(55, 55, 65)
AutoBtn.Text = "AUTO"
AutoBtn.TextColor3 = Color3.fromRGB(230, 230, 230)
AutoBtn.TextSize = 13
AutoBtn.Font = Enum.Font.GothamBold
AutoBtn.Parent = MainFrame

local AutoCorner = Instance.new("UICorner")
AutoCorner.CornerRadius = UDim.new(0, 8)
AutoCorner.Parent = AutoBtn

AutoBtn.Activated:Connect(function()
    AutoRejoin = not AutoRejoin
    AutoBtn.BackgroundColor3 = AutoRejoin and Color3.fromRGB(30, 160, 60) or Color3.fromRGB(55, 55, 65)
    StatusDot.BackgroundColor3 = AutoRejoin and Color3.fromRGB(50, 220, 90) or Color3.fromRGB(220, 50, 50)
end)

local RollbackBtn = Instance.new("TextButton")
RollbackBtn.Size = UDim2.new(1, -30, 0, 38)
RollbackBtn.Position = UDim2.new(0, 15, 0, 100)
RollbackBtn.BackgroundColor3 = Color3.fromRGB(190, 35, 35)
RollbackBtn.Text = "ROLLBACK"
RollbackBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
RollbackBtn.TextSize = 15
RollbackBtn.Font = Enum.Font.GothamBold
RollbackBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 10)
BtnCorner.Parent = RollbackBtn

local PlaceId = game.PlaceId
local JobId = game.JobId

RollbackBtn.Activated:Connect(function()
    task.wait(0.1)
    TeleportService:TeleportToPlaceInstance(PlaceId, JobId, Player)
end)

print("✅ MEOW Secured Key System Loaded!")
